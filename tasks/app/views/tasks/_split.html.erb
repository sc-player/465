<%=form_for(subtask, url: {controller: "tasks", action: "split", id: subtask.id.to_s}, remote: true) do |f|%>
  <%=f.submit("Split")%>
  <%=select_tag(:number_subtasks, options_for_select(2..10)) %>
<% end -%>
