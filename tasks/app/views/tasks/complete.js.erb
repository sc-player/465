  var el = $('#mainProgressBar');
  var width = el.prop('style')['width'];
  el.html(width)
  var id = setInterval(frame, 10);
  function frame(){
    if (width >= 100) {
      clearInterval(id);
    } else {
      width++;
      el.css('width', width);
    }
  }
